#!/bin/sh
set -e

case "$1" in
  remove|upgrade|deconfigure)
    if command -v systemctl >/dev/null 2>&1; then
      systemctl stop pistomp-audio-api.service pistomp-wifi-api.service 2>/dev/null || true
    fi
    ;;
esac

#DEBHELPER#

exit 0
